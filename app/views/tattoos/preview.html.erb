<div id="banner_img">
</div>

<!-- Masonry Container -->
<div id="masonryContainer">
    <% if @photos %> 
      <% @photos.each do |photo| %>
        <div class="masonry-brick">
          <%= link_to image_tag(photo), quickpreview_path%>
        </div>
      <% end %>
    <% end %>
</div>
