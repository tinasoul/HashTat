<!-- Need to fix scroll up feature so header does not show behind transparent nav bar.
New and edit form info pulled inconsistent -->

<h1>Edit a Tat</h1>

<%= render 'form' %>

<%= link_to 'Delete', @tattoo, method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Home', root_path %>


    